name: poc_inappfirebase
description: POC de Firebase In-App Messaging

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  firebase_core: ^0.5.0
  firebase_analytics: ^6.0.0
  firebase_in_app_messaging: ^0.2.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  #assets:
  # - assets/i18n/
  # - assets/images/

  #fonts:
  #  - family: CircularStd
  #    fonts:
  #      - asset: assets/fonts/CircularStd-Book.otf
  #      - asset: assets/fonts/CircularStd-Bold.otf
  #        weight: 700
  #      - asset: assets/fonts/CircularStd-Medium.otf
  #        weight: 500